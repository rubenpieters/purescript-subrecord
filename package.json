{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib --censor-codes=ImplicitImport,ImplicitQualifiedImport,WildcardInferredType,MissingTypeDeclaration --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^12.0.1"
  },
  "dependencies": {
    "purescript-psa": "^0.6.0"
  }
}
